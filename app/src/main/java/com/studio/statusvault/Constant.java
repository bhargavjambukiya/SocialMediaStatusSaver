package com.studio.statusvault;

public class Constant {
    /** When viewing recent WhatsApp media, {@code false} uses the merged 7-day list (same as All tab). {@code true} uses only rows still in the WhatsApp .Statuses folder. */
    public static final String EXTRA_RECENT_VIEWER_STATUS_FOLDER_ONLY = "recent_viewer_status_folder_only";

    public static final int REQUEST_ID_MULTIPLE_PERMISSIONS = 1;
    public static final String SELECTED_VIDEO_PATH = "";
    public static int statusTabSelection = 0;
}
